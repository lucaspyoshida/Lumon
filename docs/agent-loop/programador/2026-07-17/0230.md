# Implementação do programador — 2026-07-17 02:30

## Avaliação respondida

O relatório do supervisor de 2026-07-17 02:00 foi lido integralmente. Esta rodada tratou exclusivamente o P1 que impedia a aprovação final: o foco automático rolava a atividade em celular pequeno, escondia navegação e contexto, e a suíte não detectava o corte vertical. `STATUS.md` continua ausente e não há declaração de `OBJETIVO ALCANÇADO — APROVADO`.

## Crítica tratada

### P1 — foco automático ocultava navegação e contexto em 360×640

- Toda troca de tela agora normaliza a rolagem no topo e move o foco para o título semântico com `preventScroll: true`.
- Foram removidos os focos automáticos concorrentes na primeira resposta e no campo numérico. A criança continua chegando às respostas pela ordem natural de tabulação; interações explícitas continuam gerindo foco normalmente.
- A rolagem suave global foi removida porque mantinha uma animação residual ao iniciar habilidades localizadas abaixo da dobra da trilha.
- A nova regressão E2E inicia e retoma escolha, digitação, ordenação e autoavaliação em 360×640; em cada modalidade exige `scrollTop <= 1`, foco no título e retângulos inteiros para `Sair`, progresso e instrução. Escape também exige retorno previsível com cabeçalho e título da trilha visíveis.
- O cache do shell passou para `lumon-shell-2026-07-17-06`, garantindo que a correção seja oferecida como atualização da PWA.

## Alterações

- `src/app.js`: posicionamento previsível da tela e foco sem rolagem; remoção dos focos automáticos concorrentes.
- `styles/base.css`: remoção de `scroll-behavior: smooth` global.
- `tests/e2e/lumon.spec.js`: regressão vertical para quatro tipos de resposta, início, retomada e abandono/retorno.
- `service-worker.js`: versão `06` do cache do shell.

## Evidências executadas

| Evidência | Resultado |
|---|---|
| `npm run check` | PASS; lint, 21/21 testes unitários/integração e build estático |
| Regressão E2E vertical isolada | PASS; 1/1 após detectar e eliminar a rolagem suave residual |
| `npm run test:e2e:chromium` | PASS; 10/10 cenários em 46,5 s |
| Quatro tipos em 360×640 | PASS; escolha, digitação, ordenação e autoavaliação em início e retomada |
| Retorno por Escape em 360×640 | PASS; `scrollTop=0`, foco em `home-title` e cabeçalho/título inteiros |
| Navegador interno em 360×640 | PASS; `scrollTop=0`, foco em `activity-title`, `scrollWidth=clientWidth=360`; cabeçalho, Sair, progresso e instrução inteiros |
| Navegador interno em 390×844, 768×1024, 1280×720 e 844×390 | PASS; `scrollTop=0`, foco no título, sem overflow horizontal e contexto superior inteiro |
| Atualização real da PWA | PASS; worker `06` oferecido, atualização aplicada e sessão em andamento preservada |
| Console do navegador interno | PASS; 0 erros e 0 avisos |
| `git diff --check` | PASS |

Evidências visuais leves, não versionadas:

- `/private/tmp/lumon-programador-atividade-360x640-2026-07-17-0230.png`
- `/private/tmp/lumon-programador-atividade-844x390-2026-07-17-0230.png`

## Itens não atendidos e motivo

- **Safari/WebKit:** NÃO TESTADO; o engine permanece indisponível neste ambiente. Chromium/Google Chrome passou.
- **Teclado virtual físico:** NÃO TESTADO; a regressão confirma que o campo numérico não abre mais teclado por foco automático, mas não simula o teclado do sistema operacional.
- **Instalação pelo prompt do sistema:** NÃO TESTADA; registro, atualização e uso offline continuam cobertos no Chromium.
- **Observação com criança:** NÃO EXECUTADA; depende de participante humano e não deve ser simulada.
- **Traçado em canvas e som/narração:** permanecem opcionais ou decisões pendentes no plano, sem relação com a crítica P1.

## Riscos e próximos passos

1. O supervisor deve reexecutar a regressão vertical e decidir a aprovação final.
2. Validar Safari e teclado virtual em ambiente com esses recursos disponíveis e realizar observação humana.
3. Sem nova crítica P0/P1 ou lacuna objetiva do plano, não criar melhorias adicionais.
