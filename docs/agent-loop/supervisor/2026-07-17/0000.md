# Avaliação do supervisor — 2026-07-17 00:00

## Veredito

**REPROVADO**

O commit avaliado ainda é o aplicativo legado de cartões descrito pelo próprio plano de evolução. Há reconhecimento de numerais, cartões de soma/subtração e treino efêmero dos erros da rodada, mas não existem as cinco etapas completas, domínio baseado em três sessões, histórico de progresso, recomendação de avanço/revisão, modalidades de resposta exigidas, arquitetura testável ou comprovação offline/acessível. O objetivo vinculante não foi atingido.

Não foi criado `STATUS.md`: o estado não é `CONCLUÍDO`.

## Escopo e ambiente

| Item | Valor |
|---|---|
| Repositório validado | `/Users/yoshida/Documents/Codex/2026-07-16/quai/Lumon` |
| Branch validada | `feature/evolucao-pedagogica` |
| Commit avaliado | `e769e5bb5994985b9b1577e66a3c71820469b5dd` |
| Commit/data | `2026-07-16T23:29:00-03:00` — `documenta e planeja evolução do Lumon` |
| Sistema | macOS 26.5.2 (25F84), arm64 |
| Ferramentas | Git 2.55.0; Node 24.18.0; npm 11.16.0; Python 3.14.6 |
| Navegadores detectados | Google Chrome e Safari instalados; Firefox e Edge indisponíveis |
| Estado local inicial | somente `.reversa/config.user.toml` não rastreado; preservado e não incluído |
| Sincronização | `git fetch` falhou por DNS (`Could not resolve host: github.com`); refs locais indicavam `HEAD...origin/feature/evolucao-pedagogica = 0/0`, sem confirmação remota atual |
| Fonte de verdade | `_reversa_sdd/plano-evolucao-lumon.md` |

## Legenda de evidência

- **TESTADO**: comando ou inspeção executada nesta rodada.
- **FALHOU**: requisito contradito por evidência executável ou pelo código entregue.
- **NÃO TESTADO**: ambiente ou infraestrutura não permitiu comprovar.
- **INFERIDO**: conclusão estática razoável, sem validação funcional real.

## Matriz de aceite das cinco etapas

| Etapa | Estado | Evidência e conclusão |
|---|---|---|
| 1. Reconhecimento e contagem até 10 | **FALHOU — TESTADO por inspeção** | Existe apenas a fila de numerais 1–10 (`script.js:60-64,99-105`). Não há contagem/quantidades, associação em nenhum sentido, seleção de numeral, ordenação nem termo ausente. |
| 2. Sequência e quantidades até 30 | **FALHOU — TESTADO por inspeção** | Os níveis 1–20 e 1–30 são somente cartões; toda sessão comum é truncada nos primeiros 15 itens (`script.js:62-64,153`). Não existem anterior/posterior, uma ou duas lacunas, ordenação ou quantidades agrupadas. |
| 3. Leitura, seleção e escrita até 50 | **FALHOU — TESTADO por inspeção** | Não existe etapa até 50. O intervalo customizado 1–100 apenas cria cartões; não oferece alternativas, entrada numérica, tabelas, dezenas/unidades ou ordenação (`index.htm:96-110`; `script.js:322-331`). |
| 4. Adição progressiva | **FALHOU — TESTADO por inspeção** | Há cartões `+1`, `+2`, `+3` e aleatórios (`script.js:66,106-114`), mas não há progressão por resultado até 5/10, seleção, digitação, apoio visual, termo ausente, revisão por operando ou domínio separado. Os níveis fixos geram resultados até 11/12/13 e a resposta usa `eval()` (`script.js:113,193`). |
| 5. Subtração e consolidação | **FALHOU — TESTADO por inspeção** | Há cartões `-1`, `-2`, `-3` e aleatórios (`script.js:67,115-123`). Resultado não negativo é **INFERIDO** pelas faixas do gerador, mas faltam retirada visual, seleção, digitação, operações mistas, inversa, termo ausente, revisão adaptativa e domínio por operando/misto. |

## Requisitos transversais

| Requisito | Estado | Evidência |
|---|---|---|
| Domínio configurável por três sessões recentes | **FALHOU** | O estado tem somente a sessão corrente (`script.js:48-53`); não há histórico, habilidades ou cálculo de 90%/limite individual de erros. |
| Revisão de erros | **FALHOU / parcial** | `Treinar erros` reutiliza apenas os itens errados na memória da rodada (`script.js:157-169,207-215,422-440`). Não sobrevive a reload, não usa sessões recentes e não prioriza fraquezas persistidas. |
| Recomendação de avanço ou revisão | **FALHOU** | O resumo oferece somente recomeçar, treinar erros e voltar (`index.htm:87-94`); não existe motor de recomendação ou bloqueio/desbloqueio. |
| Persistência somente local | **FALHOU / parcial; privacidade INFERIDA** | Apenas atividade, modo e nível são salvos (`script.js:71-86`), não o progresso. A busca estática encontrou somente `fetch` do service worker e nenhuma API/analytics externa, portanto ausência de transmissão de desempenho é **INFERIDA**. |
| PWA offline | **FALHOU / NÃO TESTADO ponta a ponta** | Manifesto é JSON válido e os ícones têm 192×192 e 512×512 (**TESTADO**). O worker passa em sintaxe, mas usa registro absoluto `/Lumon/service-worker.js`, assets `/images/...`, não pré-cacheia ícones e apaga todo cache de outro nome (`script.js:5`; `service-worker.js:1-14,42-55`). Offline/atualização não puderam ser executados. |
| Arquitetura testável e sem `eval()` | **FALHOU** | Não há módulos, contrato de questão, manifesto de dependências ou testes; `rg` encontrou `eval()` em `script.js:193`. |
| Geradores determinísticos | **FALHOU** | Geração e embaralhamento usam `Math.random()` diretamente (`script.js:57,109-110,118-119,146-148,167`) e as funções não são exportáveis. |
| Responsividade e zoom | **FALHOU / NÃO TESTADO visualmente** | O documento proíbe zoom com `user-scalable=no` (`index.htm:5`). CSS fixa altura e `overflow:hidden`, não possui media queries/orientação (`style.css:42-76`). Os quatro viewports reais não puderam ser renderizados. |
| Acessibilidade | **FALHOU** | A atividade só aceita gesto horizontal; o cartão é `div` sem papel/foco e não existem controles equivalentes (`index.htm:43-57`; `script.js:377-453`). Busca estática não encontrou ARIA, `alt`, foco visível, `inputmode` ou redução de movimento. |
| Princípios pedagógicos/infantis | **FALHOU / parcial INFERIDO** | Botões grandes, texto curto, ausência de cronômetro e limite de até 15 existem. Não há uma habilidade por vez em trilha, domínio, apoio graduado, resposta verificável, alternativa ao gesto ou celebração/recomendação baseada em progresso. |

## Matriz mínima de testes

| Grupo | Resultado da rodada |
|---|---|
| A. Qualidade | **FALHOU.** `node --check script.js` e `node --check service-worker.js` passaram; `manifest.json` foi parseado com sucesso. Não existem instalação reproduzível, lockfile, lint, testes ou build. Busca explícita achou `eval()` e não achou `new Function`. Console real: **NÃO TESTADO**. |
| B. Unidade/domínio | **FALHOU.** Não há testes nem superfície importável. Determinismo, domínio por três sessões, revisão persistente e migração estão ausentes. Limites de subtração não negativa são **INFERIDOS**, não testados. JSON corrompido é lido por `JSON.parse` sem recuperação (`script.js:83-86`). |
| C. Cobertura pedagógica | **FALHOU.** A inspeção de toda a superfície de atividades encontrou somente cartões de numerais/letras/operações e swipe. Seleção, digitação, ordenação, sequência, termo ausente e apoios visuais não existem. Sessões comuns são limitadas a 15, mas revisões podem ter menos de 5 e não há configuração pedagógica de 5–15. |
| D. Fluxos ponta a ponta | **NÃO TESTADO e objetivamente incompleto.** Não há E2E. O navegador controlado não pôde abrir o servidor local: o ambiente negou bind de porta e a política de URL bloqueou a alternativa em memória. Avanço, bloqueio, persistência de progresso e reinício confirmado não existem no código. |
| E. Interface real | **NÃO TESTADO.** 360×640, 390×844, 768×1024 e 1280×720, orientação, teclado virtual, overflow, estados e movimento não puderam ser renderizados. A proibição de zoom é uma falha estática confirmada. |
| F. Acessibilidade | **FALHOU por inspeção; interação real NÃO TESTADA.** Botões nativos do menu são focáveis por padrão, mas não existe meio de responder por teclado, gestão de foco, anúncio, estados acessíveis, alternativa a cor/gesto/som ou redução de movimento. |
| G. PWA/offline | **FALHOU / NÃO TESTADO.** Manifesto, sintaxe e dimensões dos ícones passaram. Registro/escopo, primeira carga online + reload offline, sessão offline e atualização sem cache quebrado não foram executados; os caminhos e a limpeza ampla do cache são riscos confirmados no código. |
| H. Privacidade/robustez | **FALHOU / parcial INFERIDO.** Busca estática não encontrou transmissão de negócio, analytics ou dependência externa crítica. Progresso não é persistido. JSON inválido pode interromper o fluxo; entrada inválida, duplo clique, refresh rápido e exceções não tratadas são **NÃO TESTADOS**. |
| I. Compatibilidade/regressão | **NÃO TESTADO.** Chrome e Safari estão instalados, mas nenhum engine executou o app nesta rodada. Firefox e Edge estão indisponíveis. Não há baseline anterior do supervisor; a ref local mostra a branch no mesmo commit de `origin/main`, sem evolução de produto para comparar. |

## Problemas acionáveis

### P0 — As cinco etapas vinculantes não foram implementadas

**Reprodução:** executar `rg -n 'data-activity|const niveis' index.htm script.js` e inspecionar `script.js:60-68,94-155` e `index.htm:16-113`.

**Evidência:** a superfície oferece `Números`, `Somas`, `Subtrações` e `Letras`; os exercícios são valores/expressões em cartões. As atividades obrigatórias de quantidades, seleção, digitação, sequência/lacunas, ordenação, apoio visual, termo ausente e consolidação mista não aparecem.

**Critério de aceite:** disponibilizar as cinco etapas com todas as atividades e progressões internas obrigatórias do plano, em sessões de 5–15 exercícios, e cobri-las por testes automatizados e E2E de ponta a ponta.

### P0 — Não existe progressão por domínio nem progresso recente persistido

**Reprodução:** executar `rg -n 'localStorage|estadoAtual|errosQuestoes|mastery|completedSessions|skillMastery|weakItems|unlockedLevels' script.js`.

**Evidência:** o estado é somente a rodada atual (`script.js:48-53`) e o armazenamento mantém apenas atividade/modo/nível (`script.js:71-86`). Sair zera tudo (`script.js:218-220`). Não há três sessões recentes, limiar de 90%, limite de erro individual, abandono, recomendação, bloqueio ou desbloqueio do responsável.

**Critério de aceite:** persistir localmente schema versionado, sessões e desempenho por habilidade/item; calcular domínio configurável com pelo menos três sessões recentes; priorizar erros; recomendar revisão/avanço; bloquear/desbloquear conforme as regras; recuperar JSON inválido e migrar `lumon-last-settings` sem perder dados.

### P1 — Arquitetura e geradores não são testáveis, determinísticos ou livres de `eval()`

**Reprodução:** executar `find . -name package.json -o -name '*test*' -o -name '*spec*'` e `rg -n '\beval\s*\(|Math\.random' script.js`.

**Evidência:** não existe manifesto/lockfile, linter, build ou teste. Toda a aplicação está na closure de `script.js`; `eval()` calcula respostas (`script.js:193`) e `Math.random()` está embutido nos geradores/embaralhamento. Os níveis fixos de soma extrapolam resultado 10.

**Critério de aceite:** separar domínio, sessão, persistência, geradores e UI em módulos importáveis; remover `eval()`/`new Function`; injetar seed/fonte aleatória; garantir limites, alternativas únicas e ausência de subtração negativa; fornecer instalação reproduzível, lint, testes e build verdes.

### P1 — O fluxo de resposta não é acessível

**Reprodução:** inspecionar `index.htm:37-58`, `script.js:377-453` e executar `rg -n 'aria-|role=|tabindex=|alt=|focus-visible|prefers-reduced-motion|inputmode=' index.htm style.css script.js`.

**Evidência:** apenas swipe registra acerto/erro; o cartão é um `div` não focável. A busca não encontrou nomes/roles/estados, `aria-live`, `alt`, foco visível, redução de movimento ou entrada numérica apropriada. `user-scalable=no` impede zoom (`index.htm:5`).

**Critério de aceite:** concluir todos os fluxos apenas com teclado; suportar Enter/Espaço/Escape, ordem e foco visível; fornecer controles equivalentes ao gesto; expor nomes/roles/estados; anunciar feedback/progresso; não depender só de cor/gesto/som; permitir zoom e redução de movimento; validar contraste.

### P1 — Offline e atualização da PWA não estão comprovados e têm caminhos inseguros

**Reprodução:** inspecionar `script.js:1-9`, `service-worker.js:1-55` e `manifest.json`; em servidor de escopo real, executar primeira carga online, desligar rede e recarregar/iniciar/concluir sessão.

**Evidência:** registro e assets misturam `/Lumon/...`, `/images/...` e caminhos relativos; ícones não entram no pré-cache; o `activate` exclui qualquer cache cujo nome não seja o atual, inclusive de outros apps da origem. A rodada não conseguiu executar o ciclo offline.

**Critério de aceite:** usar caminhos coerentes com base/escopo, cache prefixado pelo Lumon, fallback de navegação e estratégia versionada de atualização; provar instalação, reload e sessão offline após primeira carga e atualização preservando progresso.

### P2 — Persistência não se recupera de dados locais inválidos

**Reprodução:** gravar texto que não seja JSON em `localStorage['lumon-last-settings']`, recarregar, escolher uma atividade comum e selecionar um modo.

**Evidência:** `loadLastUsedSettings()` chama `JSON.parse(savedSettings)` sem `try/catch`, validação ou fallback (`script.js:83-86`); o fluxo invoca essa função em `script.js:255`.

**Critério de aceite:** validar schema, manter backup temporário na migração, recuperar padrões seguros sem exceção e cobrir JSON corrompido, migração e exclusão por testes.

### P2 — Responsividade e compatibilidade carecem de evidência executável

**Reprodução:** executar a matriz real em 360×640, 390×844, 768×1024 e 1280×720, retrato/paisagem quando aplicável, com zoom e teclado virtual; repetir em Chromium e Safari.

**Evidência:** não há automação E2E ou regressão visual. CSS usa altura fixa e `overflow:hidden` sem media queries. A tentativa desta rodada foi bloqueada pelo ambiente antes de carregar a aplicação.

**Critério de aceite:** anexar resultados/screenshot leves para todos os viewports, sem overflow ou controles cortados, com toque ≥48 px, zoom funcional, teclado virtual e redução de movimento; executar pelo menos Chromium e registrar explicitamente engines indisponíveis.

## Regressões

- **NÃO TESTADO:** não existe suite automatizada nem relatório anterior do supervisor.
- A ref local de `feature/evolucao-pedagogica` e a ref local de `origin/main` apontavam para o mesmo commit. Assim, não havia delta de produto implementado a validar; a única mudança observada no commit foi documentação/estrutura do Reversa.
- Nenhum arquivo do aplicativo foi alterado nesta rodada.

## Dúvidas

Nenhuma dúvida impede a implementação. O objetivo vinculante e os critérios globais do plano são suficientes para a próxima rodada do programador. Itens opcionais do plano, como traçado em canvas, não foram convertidos em bloqueadores.

## Evidências de comandos

```text
PASS  git rev-parse --show-toplevel = /Users/yoshida/Documents/Codex/2026-07-16/quai/Lumon
PASS  git branch --show-current = feature/evolucao-pedagogica
PASS  node --check script.js
PASS  node --check service-worker.js
PASS  python3 -m json.tool manifest.json
PASS  sips: icon-192x192.png = 192x192; icon-512x512.png = 512x512
FAIL  rg por eval/new Function: script.js:193 usa eval(questao)
FAIL  infraestrutura: nenhum package.json, lockfile, teste, linter, build ou E2E encontrado
BLOCK git fetch: Could not resolve host: github.com
BLOCK git push: Could not resolve host: github.com; publicação remota não confirmada
BLOCK servidor local: bind de porta negado pelo ambiente
BLOCK navegador: URL local/alternativa em memória bloqueada pela política de segurança
```

## Encerramento do ciclo desta rodada

O ciclo foi encerrado neste ponto porque já existem falhas P0 comprovadas contra o objetivo explícito. Procurar defeitos adicionais não mudaria o veredito. As automações não devem ser pausadas: o objetivo ainda não foi alcançado e não há `STATUS CONCLUÍDO` publicado.
